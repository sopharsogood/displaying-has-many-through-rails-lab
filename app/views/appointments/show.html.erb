<h1>Doctor: <%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %></h1>
<h2>Patient: <%= link_to @appointment.patient.name, patient_path(@appointment.patient) %></h2>

<p><%= @appointment.appointment_datetime.strftime("%B %_d, %Y at %k:%M") %></p>